import pandas as pd;

data = [10, 20, 30, 40, 50, 60]
indices = list('abcdef')

srs = pd.Series(data=data, index=indices)

# print(srs)
# print(srs['c'])
# print(srs['d':])

print(srs.iloc[1])
print(srs.iloc[1:3])

print()
print(srs.loc['b'])